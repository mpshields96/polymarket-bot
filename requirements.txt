# polymarket-bot dependencies — pinned for reproducibility
# Install via: source venv/bin/activate && pip install -r requirements.txt

# === Core async HTTP + WebSocket ===
aiohttp==3.9.5
websockets==12.0

# === Kalshi RSA-PSS auth ===
cryptography==42.0.8

# === Config + environment ===
python-dotenv==1.0.1
pyyaml==6.0.1
pydantic-settings==2.3.4
pydantic==2.7.4

# === Dashboard ===
streamlit==1.36.0

# === Database ===
# SQLite is stdlib — no extra package needed

# === Testing ===
pytest==8.2.2
pytest-asyncio==0.23.7

# === Synchronous HTTP (status command, Binance REST snapshot) ===
requests==2.32.5

# === Polymarket (build now, activate when off waitlist) ===
eth-account==0.11.3
